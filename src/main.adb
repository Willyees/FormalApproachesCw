with submarine; use submarine;
with Reactor; use Reactor;
with Air; use Air;
with Door; use Door;
with Ada.Text_IO; use Ada.Text_IO;

procedure Main is

   --sub : submarine.Submarine;
   --a : array_u := Door.Create(10);
   --d : Door.Doors;
begin
   Put_Line("main");

end Main;



